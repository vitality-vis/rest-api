# Compatibility shim only. Package versions live in pyproject.toml.
# TODO: remove this file once all install/deploy paths use pyproject extras directly,
# e.g. `pip install -e ".[full,rerank]"` or `pip install -e ".[full,rerank,dev]"`.
-e .[full,rerank]
