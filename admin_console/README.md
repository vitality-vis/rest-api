# Admin Console

Static browser UI for browsing Azure OpenAI **Files** and **Vector Stores** (login with endpoint + API key; credentials stay in localStorage).

## Run

```bash
cd rest-api/admin_console
python -m http.server 8765
```

Open http://localhost:8765
